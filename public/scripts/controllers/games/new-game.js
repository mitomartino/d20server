'use strict';

angular.module('d20helper.games.newGame', ['ngRoute']).

/**
 * Controller for creating a new game
 *
 * Manages creation of games for a user
 */
controller('GamesNewGameCtrl', ['$scope', '$timeout', 'modalService', 'gameService', 'constantsService', 'applicationService',

    function($scope, $timeout, modalService, gameService, constants, applicationService)
    {
        /**
         * Initialize the controller
         */
        function init()
        {

        }
    }
]);
